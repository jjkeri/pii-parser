## Project: pii-parser
Python PII sanitiser for enterprise support workflows.
Entry point: pii_parser.py → clean_case(text)
Run tests: python -m pytest tests/ (if tests/ exists)
Style: PEP8, type hints on all functions, docstrings on public functions.
Goal: keep it simple, dependency-light, and easy to extend via config.
